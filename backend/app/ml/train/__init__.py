"""Offline training scripts (TRD §5, Tier 1+ optional classifier).

Empty by design in Tier 1: scoring is unsupervised — every threshold is derived from the
patient's own baseline, so no trained model is required for the shipped pipeline.
"""
