{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'timeout 120 alembic upgrade head > /tmp/boot.log 2>&1; echo ALEMBIC_EXIT=$? >> /tmp/boot.log; timeout 90 uvicorn app.main:app --host :: --port ${PORT} >> /tmp/boot.log 2>&1; echo UVICORN_EXIT=$? >> /tmp/boot.log; cd /tmp && exec python -m http.server ${PORT}'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "numReplicas": 1
  }
}
